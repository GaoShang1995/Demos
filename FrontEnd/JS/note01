1.var声明的变量，会在所有代码执行之前被声明
    但是不会赋值，直到显式赋值
    如果声明b变量时不适用var关键字，则变量不会被提前声明
2.函数声明提前
    使用函数声明形式创建的函数function 函数（）{}
        它会在所有代码执行之前创建，所以可以在函数声明前调用
        函数表达式var a = function(){}，a会初始化为undefined，没办法在显式声明前调用
3.全局变量和函数，可以在任何地方访问，默认为window对象的属性和方法