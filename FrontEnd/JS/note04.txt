事件的传播：
        事件传播分为了三个阶段：   
            1.捕获阶段
            在捕获阶段时从最外层的祖先元素，向目标元素进行事件的捕获，但是默认此时不会触发事件
            2.目标阶段
            事件捕获到目标元素，捕获结束开始在目标元素上触发事件
            3.冒泡阶段
            事件从目标元素向祖先元素传递，分别依次触发祖先元素上的事件
        如果希望，在事件捕获阶段，就触发事件，可以将addEventListener()的第三个参数，设置为true